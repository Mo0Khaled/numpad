name: numpad_layout
description: A new Flutter Package for numeric Keyboard with flexible customization as well as arabic digits support and many many more.
version: 0.0.3
homepage: https://github.com/Mo0Khaled/numpad

environment:
  sdk: '>=2.18.1 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter: